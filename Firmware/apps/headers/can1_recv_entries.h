//Only need the size to be 1 for latest, but may want to increase this for logging purposes
//Makefile needs to point to this for FSM entries

//Motor CAN

// CAN_RECV_ENTRY(CAN_ID_MOTOR_DRIVE, 1, true)
// CAN_RECV_ENTRY(CAN_ID_MOTOR_POWER, 1, true)
// CAN_RECV_ENTRY(CAN_ID_MOTOR_RESET, 1, true)
// CAN_RECV_ENTRY(CAN_ID_MOTOR_IDENTIFICATION, 1, true)
// CAN_RECV_ENTRY(CAN_ID_MOTOR_STATUS, 1, true)