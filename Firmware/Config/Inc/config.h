#pragma once
#include "common.h"

//--------------------------------------------------------------------------------
//--------------------------------------------------------------------------------

// VCU configurations
// Each VCU configuration defines stuff
// (VoltTemp ID, {Battery Modules connected}, CAN ID)
#define VCU_CONFIGS 
   //make a function that takes 0-99 to display on the seven segment
